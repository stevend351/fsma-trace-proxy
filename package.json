{
  "name": "fsma-trace-proxy",
  "version": "1.0.0",
  "description": "FSMA traceability proxy: phone -> Airtable Inventory NEW. Holds the PAT server-side.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  }
}
